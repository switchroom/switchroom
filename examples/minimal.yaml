# clerk.yaml — Minimal single-agent setup
# One bot, one agent, two lines per agent.

clerk:
  version: 1

telegram:
  bot_token: "vault:telegram-bot-token"
  forum_chat_id: "-1001234567890"

memory:
  backend: hindsight
  shared_collection: shared

defaults:
  model: claude-sonnet-4-6
  channels:
    telegram:
      plugin: clerk
  tools:
    allow: [all]

agents:
  assistant:
    topic_name: "General"
