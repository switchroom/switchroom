- **memory: Memory v2 M1 — rules-block toolchain, built dark (#4758)** — a marker-delimited, hash-chained standing-rules block for CLAUDE.md (`switchroom memory rule add|retire|list|edit-yours|verify`, four matching `agent-config` MCP tools, a doctor integrity/divergence check, and a read-only SessionStart tamper sentinel). Gated by `memory.rules_block` (default `false`): off, behavior is byte-identical to pre-M1 — no rendered block, no CLAUDE.md Edit/Write deny, zero doctor rows, sentinel hook fast-no-ops. Only a future M3 rollout flips the flag per agent.
