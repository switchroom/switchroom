{
  "name": "clerk-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing clerk management tools to Claude Code agents",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
