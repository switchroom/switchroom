# MEMORY.md — Long-Term Memory

> **Detail files** (searchable via `memory_search` when available):
> - `memory/YYYY-MM-DD.md` — daily notes / raw logs
> - Add other curated memory files here as you create them.

## People

{{#if user.name}}
- **{{user.name}}** — {{#if user.callThem}}call them {{user.callThem}}{{/if}}{{#if user.location}}, lives in {{user.location}}{{/if}}. _Update this with anything worth keeping long-term._
{{else}}
_Add short profiles of the people you interact with. Distilled, not raw._
{{/if}}

## Decisions

_Record non-obvious decisions here with the date and rationale. When a
future-you asks "why did I do it this way?", this is where the answer
lives._

## Lessons learned

_When you get something wrong, or find a better way, write it down. This
is how you stop repeating mistakes._

## Open threads

_Ongoing projects or questions that span multiple sessions. One line per
thread with enough context to pick it back up._
